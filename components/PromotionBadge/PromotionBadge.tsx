/** Renderuje znacznik informujący o promocji produktu. */
export function PromotionBadge() {
  return (
    <div className="absolute left-4 top-4 rounded-full bg-cd-dark px-4 py-2 text-[12px] uppercase tracking-[0.18em] text-white">
      PROMOCJA
    </div>
  );
}
